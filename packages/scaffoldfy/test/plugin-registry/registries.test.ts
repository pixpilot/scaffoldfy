/**
 * Tests for plugin registries
 *
 * Note: The registries (pluginRegistry, hooksRegistry, taskTypeToPlugin) are internal
 * implementation details and are not exported. They are tested indirectly through
 * the functions that use them in the other test files.
 */
