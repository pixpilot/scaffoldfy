/**
 * Tests for copy plugin types
 */

import { describe, expect, it } from 'vitest';

describe('copy plugin types', () => {
  it('should have types', () => {
    // Placeholder test for types
    expect(true).toBe(true);
  });
});
