/**
 * Tests for mkdir plugin types
 */

import { describe, expect, it } from 'vitest';

describe('mkdir plugin types', () => {
  it('should have types', () => {
    // Placeholder test for types
    expect(true).toBe(true);
  });
});
