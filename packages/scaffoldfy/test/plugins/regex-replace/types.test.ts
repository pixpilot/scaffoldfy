/**
 * Tests for regex-replace plugin types
 */

import { describe, expect, it } from 'vitest';

describe('regex-replace plugin types', () => {
  it('should have types', () => {
    // Placeholder test for types
    expect(true).toBe(true);
  });
});
