/**
 * Tests for move plugin types
 */

import { describe, expect, it } from 'vitest';

describe('move plugin types', () => {
  it('should have types', () => {
    // Placeholder test for types
    expect(true).toBe(true);
  });
});
