/**
 * Tests for delete plugin types
 */

import { describe, expect, it } from 'vitest';

describe('delete plugin types', () => {
  it('should have types', () => {
    // Placeholder test for types
    expect(true).toBe(true);
  });
});
