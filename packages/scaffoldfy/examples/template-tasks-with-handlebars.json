{
  "$schema": "../schema/tasks.schema.json",
  "name": "template-with-handlebars",
  "description": "Demonstrates using Handlebars for advanced templating with helpers and partials",
  "tasks": [
    {
      "id": "clean-readme-inline",
      "name": "Clean README (Inline)",
      "description": "Create a fresh README using inline template",
      "required": true,
      "enabled": true,
      "type": "write",
      "config": {
        "file": "README.md",
        "template": "# {{projectName}}\n\n> Created by {{author}}\n\n## Repository\n\n- Owner: {{owner}}\n- URL: {{repoUrl}}\n"
      }
    },
    {
      "id": "clean-readme-file",
      "name": "Clean README (File-based)",
      "description": "Create a fresh README using external template file",
      "required": true,
      "enabled": false,
      "type": "write",
      "config": {
        "file": "README.md",
        "templateFile": "examples/templates/readme.hbs"
      }
    },
    {
      "id": "simple-template",
      "name": "Simple Template (Legacy)",
      "description": "Simple template using {{var}} syntax without Handlebars",
      "required": true,
      "enabled": false,
      "type": "write",
      "config": {
        "file": "SIMPLE.md",
        "template": "# {{projectName}}\nAuthor: {{author}}\n"
      }
    },
    {
      "id": "simple-template-file",
      "name": "Simple Template from File (Legacy)",
      "description": "Simple template from file using {{var}} syntax without Handlebars",
      "required": true,
      "enabled": false,
      "type": "write",
      "config": {
        "file": "OUTPUT.md",
        "templateFile": "examples/templates/readme.hbs"
      }
    }
  ]
}
