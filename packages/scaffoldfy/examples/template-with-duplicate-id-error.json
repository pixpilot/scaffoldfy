{
  "$schema": "../schema/tasks.schema.json",
  "name": "template-with-duplicate-id-error",
  "description": "Example that demonstrates duplicate task ID validation error",
  "tasks": [
    {
      "id": "projectName",
      "name": "Setup Project",
      "description": "This will fail because the ID 'projectName' conflicts with the prompt below",
      "required": true,
      "enabled": true,
      "type": "write",
      "config": {
        "file": "README.md",
        "template": "# {{projectName}}"
      }
    }
  ],
  "prompts": [
    {
      "id": "projectName",
      "type": "input",
      "message": "What is your project name?",
      "required": true
    }
  ]
}
