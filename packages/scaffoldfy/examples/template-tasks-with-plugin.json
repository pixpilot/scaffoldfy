{
  "name": "template-with-custom-plugin",
  "description": "Demonstrates using a custom plugin for specialized tasks",
  "tasks": [
    {
      "id": "setup-docker",
      "name": "Setup Docker",
      "description": "Create Docker configuration files",
      "required": true,
      "enabled": true,
      "type": "docker-setup",
      "config": {
        "baseImage": "node:20-alpine",
        "port": 3000,
        "workdir": "/app",
        "installCommand": "pnpm install --frozen-lockfile",
        "startCommand": "[\"pnpm\", \"start\"]"
      }
    },
    {
      "id": "update-readme",
      "name": "Update README",
      "description": "Add Docker instructions to README",
      "required": false,
      "enabled": true,
      "type": "write",
      "dependencies": ["setup-docker"],
      "config": {
        "file": "README.md",
        "template": "# {{projectName}}\n\n## Development\n\n### With Docker\n\n```bash\ndocker-compose up\n```\n\n### Local\n\n```bash\npnpm install\npnpm dev\n```\n\n## Author\n\n{{author}}"
      }
    }
  ]
}
