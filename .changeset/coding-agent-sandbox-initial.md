---
'@pixpilot/coding-agent-sandbox': minor
'@pixpilot/scaffoldfy-configs': minor
---

Add `@pixpilot/coding-agent-sandbox`: a CLI that runs Claude Code, OpenAI Codex or GitHub Copilot CLI inside Docker against a dedicated Git worktree, with agent-scoped branches (`ai/<agent>/<task>`), read-only centralized skills provisioning, persistent per-agent OAuth volumes and an interactive foreground session. Adds the matching `coding-agent-sandbox` Scaffoldfy config.
